# 极简控糖 App（XueTangJiLu）技术架构文档

> **版本：** v1.0  
> **日期：** 2026-02-12  
> **作者：** 开发团队  
> **状态：** 初始草案

---

## 目录

1. [项目总览与技术栈](#1-项目总览与技术栈)
2. [项目目录结构](#2-项目目录结构)
3. [数据模型设计](#3-数据模型设计)
4. [HealthKit 集成方案](#4-healthkit-集成方案)
5. [CloudKit / SwiftData 同步架构](#5-cloudkit--swiftdata-同步架构)
6. [核心业务逻辑](#6-核心业务逻辑)
7. [Widget / Siri Shortcuts 实现](#7-widget--siri-shortcuts-实现)
8. [数据隐私与合规](#8-数据隐私与合规)
9. [性能优化策略](#9-性能优化策略)
10. [测试策略](#10-测试策略)

---

## 1. 项目总览与技术栈

### 1.1 产品定位

XueTangJiLu（学糖记录）是一款面向 iOS 平台的极简血糖管理工具，核心定位为**纯数字日志**（Digital Logger），不提供任何医疗诊断或治疗建议。产品目标是让用户在 **3 秒内**完成一次血糖记录。

### 1.2 技术栈总览

| 层级 | 技术选型 | 版本要求 | 选型理由 |
|:-----|:---------|:---------|:---------|
| **语言** | Swift | 5.9+ | 最新并发模型、宏支持 |
| **UI 框架** | SwiftUI | iOS 17+ | 原生声明式 UI，天然支持 Dark Mode / Dynamic Type |
| **数据持久化** | SwiftData | iOS 17+ | Apple 官方 ORM，与 CloudKit 无缝集成 |
| **云同步** | CloudKit (Private Database) | iOS 17+ | 零后端成本，数据主权归用户，中国区由云上贵州运营 |
| **健康数据** | HealthKit | iOS 17+ | 血糖数据读写，与 Apple Health 生态打通 |
| **图表** | Swift Charts | iOS 16+ | 官方图表框架，自带 VoiceOver 无障碍支持 |
| **小组件** | WidgetKit | iOS 17+ | 桌面 / 锁屏小组件 |
| **语音交互** | App Intents | iOS 17+ | Siri Shortcuts 语音录入 |
| **PDF 导出** | UIGraphicsPDFRenderer | iOS 10+ | 系统原生 PDF 生成 |

### 1.3 最低部署目标

**iOS 17.0**

> **决策依据：** SwiftData 最低要求 iOS 17；Swift Charts 从 iOS 16 开始可用但在 iOS 17 获得重大改进；iOS 17 的全球安装率已超过 90%。将部署目标从项目模板的 iOS 26 下调至 iOS 17，可最大化用户覆盖率。

### 1.4 架构模式：MVVM + Service Layer

```
┌──────────────────────────────────────────────────┐
│                    View Layer                     │
│         (SwiftUI Views / Widgets)                 │
└──────────────────┬───────────────────────────────┘
                   │ @Observable / @Bindable
┌──────────────────▼───────────────────────────────┐
│                ViewModel Layer                    │
│    GlucoseVM │ ChartVM │ SettingsVM │ OnboardVM  │
└──────┬───────────┬───────────┬───────────────────┘
       │           │           │
┌──────▼───────────▼───────────▼───────────────────┐
│                Service Layer                      │
│  HealthKitManager │ PDFExportService │ TagEngine  │
└──────┬───────────────────────┬───────────────────┘
       │                       │
┌──────▼───────────────────────▼───────────────────┐
│              Data Layer                           │
│        SwiftData (Local + CloudKit)               │
│        HealthKit Store                            │
└──────────────────────────────────────────────────┘
```

**核心原则：**

- **View** 仅负责 UI 渲染，不包含业务逻辑
- **ViewModel** 使用 `@Observable` 宏（iOS 17 Observation 框架），持有状态并调用 Service
- **Service** 封装系统框架交互（HealthKit、PDF 生成等），可独立测试
- **Model** 为 SwiftData `@Model` 类，同时定义领域逻辑（如血糖范围判定）

---

## 2. 项目目录结构

```
XueTangJiLu/
├── XueTangJiLu/                        # 主 App Target
│   ├── App/
│   │   └── XueTangJiLuApp.swift        # @main 入口，ModelContainer 配置
│   │
│   ├── Models/                          # SwiftData 数据模型
│   │   ├── GlucoseRecord.swift          # 血糖记录模型
│   │   ├── UserSettings.swift           # 用户偏好设置模型
│   │   └── Enums/
│   │       ├── GlucoseUnit.swift        # mmol/L | mg/dL 枚举
│   │       └── MealContext.swift        # 餐前/餐后/空腹/睡前/其他 枚举
│   │
│   ├── ViewModels/                      # 业务逻辑层
│   │   ├── GlucoseViewModel.swift       # 录入/列表/删除逻辑
│   │   ├── ChartViewModel.swift         # 图表数据聚合
│   │   ├── SettingsViewModel.swift      # 设置管理
│   │   └── OnboardingViewModel.swift    # 引导流程
│   │
│   ├── Views/                           # 视图层
│   │   ├── MainTabView.swift            # 底部 TabView 容器
│   │   ├── Home/
│   │   │   ├── HomeView.swift           # 首页（记录流）
│   │   │   └── TimelineRowView.swift    # 时间轴列表行
│   │   ├── Record/
│   │   │   ├── RecordInputView.swift    # 快速录入页
│   │   │   └── MinimalKeypadView.swift  # 自定义数字键盘
│   │   ├── Charts/
│   │   │   ├── TrendView.swift          # 趋势概览页
│   │   │   ├── TrendLineChart.swift     # 折线图组件
│   │   │   └── StatCardView.swift       # 统计指标卡片
│   │   ├── Settings/
│   │   │   ├── SettingsView.swift       # 设置主页
│   │   │   ├── UnitPickerView.swift     # 单位选择
│   │   │   └── AboutView.swift          # 关于/免责声明
│   │   ├── Onboarding/
│   │   │   └── OnboardingView.swift     # 首次启动引导
│   │   ├── Export/
│   │   │   └── PDFPreviewView.swift     # PDF 报告预览
│   │   └── Components/                  # 可复用组件
│   │       ├── GlucoseValueBadge.swift  # 带色彩语义的数值标签
│   │       ├── MealContextTag.swift     # 场景标签胶囊
│   │       └── EmptyStateView.swift     # 空状态占位图
│   │
│   ├── Services/                        # 服务层
│   │   ├── HealthKitManager.swift       # HealthKit 读写封装
│   │   ├── PDFExportService.swift       # PDF 报告生成
│   │   ├── TagEngine.swift              # 场景标签自动匹配引擎
│   │   └── GlucoseCalculator.swift      # eAG / TIR / A1C 计算
│   │
│   ├── Utilities/                       # 工具类
│   │   ├── Constants.swift              # 全局常量（颜色、间距、血糖范围阈值）
│   │   ├── GlucoseUnitConverter.swift   # mmol/L ↔ mg/dL 双向转换
│   │   └── Extensions/
│   │       ├── Date+Helpers.swift       # 日期格式化扩展
│   │       ├── Color+Theme.swift        # 主题色彩扩展
│   │       └── View+Haptics.swift       # 触觉反馈便利方法
│   │
│   ├── Resources/
│   │   ├── Assets.xcassets/             # 图片、颜色资源
│   │   ├── Info.plist                   # 权限声明
│   │   ├── XueTangJiLu.entitlements     # CloudKit / HealthKit 权限
│   │   └── PrivacyInfo.xcprivacy        # 隐私清单
│   │
│   └── Preview Content/                 # SwiftUI 预览数据
│       └── PreviewSampleData.swift
│
├── XueTangJiLuWidget/                   # Widget Extension Target
│   ├── XueTangJiLuWidgetBundle.swift
│   ├── SmallWidget.swift                # 小号：最新读数
│   ├── MediumWidget.swift               # 中号：读数 + 趋势线
│   ├── LockScreenWidget.swift           # 锁屏：TIR 进度环
│   └── WidgetTimelineProvider.swift     # 数据提供者
│
├── XueTangJiLuIntents/                  # App Intents Target
│   └── RecordGlucoseIntent.swift        # Siri 语音录入 Intent
│
├── XueTangJiLuTests/                    # 单元测试
│   ├── ViewModelTests/
│   ├── ServiceTests/
│   └── UtilityTests/
│
└── XueTangJiLuUITests/                  # UI 测试
    └── RecordFlowUITests.swift
```

---

## 3. 数据模型设计

### 3.1 核心模型：GlucoseRecord

```swift
import Foundation
import SwiftData

/// 血糖记录数据模型
/// 遵循 CloudKit 同步规则：所有属性均为 Optional 或提供默认值，无 unique 约束
@Model
final class GlucoseRecord {

    // MARK: - 核心数据

    /// 血糖数值（以 mmol/L 为内部统一存储单位）
    var value: Double = 0.0

    /// 记录时间戳
    var timestamp: Date = Date.now

    // MARK: - 元数据

    /// 用餐场景（餐前/餐后/空腹/睡前/其他）
    /// 使用 rawValue 字符串存储以兼容 CloudKit
    var mealContextRawValue: String = MealContext.other.rawValue

    /// 用户备注（如"吃了火锅"、"运动后"）
    var note: String?

    /// 数据来源标识（区分手动录入 vs HealthKit 导入）
    var source: String = "manual"

    /// 是否已同步到 HealthKit
    var syncedToHealthKit: Bool = false

    /// 创建时间（用于 CloudKit 冲突解决）
    var createdAt: Date = Date.now

    // MARK: - 计算属性

    /// 用餐场景枚举转换
    var mealContext: MealContext {
        get { MealContext(rawValue: mealContextRawValue) ?? .other }
        set { mealContextRawValue = newValue.rawValue }
    }

    /// 将内部 mmol/L 值转换为 mg/dL
    var valueInMgDL: Double {
        value * 18.0182
    }

    /// 血糖状态判定（基于通用范围）
    var glucoseLevel: GlucoseLevel {
        switch value {
        case ..<3.9:    return .low        // 低血糖
        case 3.9..<7.0: return .normal     // 正常范围
        case 7.0..<10.0: return .high      // 偏高
        default:         return .veryHigh  // 严重偏高
        }
    }

    // MARK: - 初始化

    init(value: Double,
         timestamp: Date = .now,
         mealContext: MealContext = .other,
         note: String? = nil,
         source: String = "manual") {
        self.value = value
        self.timestamp = timestamp
        self.mealContextRawValue = mealContext.rawValue
        self.note = note
        self.source = source
        self.createdAt = .now
    }
}
```

### 3.2 枚举定义

```swift
/// 血糖单位
enum GlucoseUnit: String, Codable, CaseIterable {
    case mmolL = "mmol/L"
    case mgdL  = "mg/dL"

    /// 根据系统地区自动推断默认单位
    /// 美国、日本等使用 mg/dL，中国、欧洲等使用 mmol/L
    static var systemDefault: GlucoseUnit {
        let regionCode = Locale.current.region?.identifier ?? "CN"
        let mgdLRegions = ["US", "JP", "IN", "CO", "IL"]
        return mgdLRegions.contains(regionCode) ? .mgdL : .mmolL
    }
}

/// 用餐场景
enum MealContext: String, Codable, CaseIterable {
    case beforeBreakfast = "breakfast_before"  // 早餐前
    case afterBreakfast  = "breakfast_after"   // 早餐后
    case beforeLunch     = "lunch_before"      // 午餐前
    case afterLunch      = "lunch_after"       // 午餐后
    case beforeDinner    = "dinner_before"     // 晚餐前
    case afterDinner     = "dinner_after"      // 晚餐后
    case fasting         = "fasting"           // 空腹
    case bedtime         = "bedtime"           // 睡前
    case other           = "other"             // 其他

    /// 本地化显示名称
    var displayName: String {
        switch self {
        case .beforeBreakfast: return "早餐前"
        case .afterBreakfast:  return "早餐后"
        case .beforeLunch:     return "午餐前"
        case .afterLunch:      return "午餐后"
        case .beforeDinner:    return "晚餐前"
        case .afterDinner:     return "晚餐后"
        case .fasting:         return "空腹"
        case .bedtime:         return "睡前"
        case .other:           return "其他"
        }
    }

    /// SF Symbol 图标名
    var iconName: String {
        switch self {
        case .beforeBreakfast, .afterBreakfast: return "sunrise"
        case .beforeLunch, .afterLunch:         return "sun.max"
        case .beforeDinner, .afterDinner:       return "sunset"
        case .fasting:                          return "moon.zzz"
        case .bedtime:                          return "bed.double"
        case .other:                            return "clock"
        }
    }
}

/// 血糖水平状态
enum GlucoseLevel {
    case low       // < 3.9 mmol/L
    case normal    // 3.9 - 6.9 mmol/L
    case high      // 7.0 - 9.9 mmol/L
    case veryHigh  // >= 10.0 mmol/L

    /// 对应的语义色彩（Constants 中定义）
    var colorName: String {
        switch self {
        case .low:      return "glucoseLow"      // 番茄红
        case .normal:   return "glucoseNormal"    // 薄荷绿
        case .high:     return "glucoseHigh"      // 向日葵黄
        case .veryHigh: return "glucoseVeryHigh"  // 番茄红
        }
    }
}
```

### 3.3 用户设置模型

```swift
@Model
final class UserSettings {

    /// 首选血糖单位
    var preferredUnitRawValue: String = GlucoseUnit.systemDefault.rawValue

    /// 目标血糖下限 (mmol/L)
    var targetLow: Double = 3.9

    /// 目标血糖上限 (mmol/L)
    var targetHigh: Double = 7.0

    /// 是否已完成引导
    var hasCompletedOnboarding: Bool = false

    /// 是否启用 HealthKit 同步
    var healthKitSyncEnabled: Bool = false

    /// 是否启用智能标签
    var autoTagEnabled: Bool = true

    // MARK: - 计算属性

    var preferredUnit: GlucoseUnit {
        get { GlucoseUnit(rawValue: preferredUnitRawValue) ?? .mmolL }
        set { preferredUnitRawValue = newValue.rawValue }
    }

    init() {}
}
```

### 3.4 数据模型关系图

```
┌─────────────────────────────────┐
│         GlucoseRecord           │
├─────────────────────────────────┤
│ value: Double                   │
│ timestamp: Date                 │
│ mealContextRawValue: String     │
│ note: String?                   │
│ source: String                  │
│ syncedToHealthKit: Bool         │
│ createdAt: Date                 │
├─────────────────────────────────┤
│ → mealContext: MealContext      │ (computed)
│ → glucoseLevel: GlucoseLevel   │ (computed)
│ → valueInMgDL: Double          │ (computed)
└─────────────────────────────────┘
        │
        │ CloudKit Sync (Private DB)
        ▼
┌─────────────────────────────────┐
│        iCloud (云上贵州)          │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│         UserSettings            │
├─────────────────────────────────┤
│ preferredUnitRawValue: String   │
│ targetLow: Double               │
│ targetHigh: Double              │
│ hasCompletedOnboarding: Bool    │
│ healthKitSyncEnabled: Bool      │
│ autoTagEnabled: Bool            │
└─────────────────────────────────┘
```

> **CloudKit 兼容性要点：**
> - 所有属性均有默认值或为 Optional
> - 不使用 `@Attribute(.unique)` 约束
> - 不使用有序关系（Ordered Relationship）
> - 枚举类型通过 rawValue 字符串存储
> - 遵循"只增不删不改"数据迁移原则

---

## 4. HealthKit 集成方案

### 4.1 架构定位

HealthKit 在本应用中扮演**辅助角色**，而非主数据库：

```
用户录入 → SwiftData (主存储) → HealthKit (同步写入)
                ↓
           CloudKit (跨设备同步)
```

**设计决策：** SwiftData 为主存储，因为 HealthKit 对自定义元数据（用餐标签、备注）的支持有限。核心血糖数值在保存到 SwiftData 后，同步写入 HealthKit，使数据与 Apple Health 生态互通。

### 4.2 HealthKitManager 实现

```swift
import HealthKit

@Observable
final class HealthKitManager {

    // MARK: - 属性

    private let healthStore = HKHealthStore()
    private(set) var isAuthorized = false

    /// HealthKit 是否可用（iPad 不支持）
    var isAvailable: Bool {
        HKHealthStore.isHealthDataAvailable()
    }

    // MARK: - 权限请求

    /// 请求血糖数据读写权限
    func requestAuthorization() async throws {
        guard isAvailable else { return }

        let bloodGlucoseType = HKQuantityType(.bloodGlucose)

        let typesToShare: Set<HKSampleType> = [bloodGlucoseType]
        let typesToRead: Set<HKObjectType> = [bloodGlucoseType]

        try await healthStore.requestAuthorization(
            toShare: typesToShare,
            read: typesToRead
        )
        isAuthorized = true
    }

    // MARK: - 写入血糖数据

    /// 将血糖记录写入 HealthKit
    /// - Parameters:
    ///   - value: 血糖数值 (mmol/L)
    ///   - date: 记录时间
    ///   - mealContext: 用餐场景
    func saveGlucose(value: Double, date: Date, mealContext: MealContext) async throws {
        guard isAvailable, isAuthorized else { return }

        let bloodGlucoseType = HKQuantityType(.bloodGlucose)
        let unit = HKUnit.moleUnit(with: .milli, molarMass: HKUnitMolarMassBloodGlucose)
            .unitDivided(by: .liter())
        let quantity = HKQuantity(unit: unit, doubleValue: value)

        // 构建元数据
        var metadata: [String: Any] = [
            HKMetadataKeyWasUserEntered: true
        ]

        // 映射用餐时间到 HealthKit 枚举值
        if let hkMealTime = mealContext.healthKitMealTime {
            metadata[HKMetadataKeyBloodGlucoseMealTime] = hkMealTime
        }

        let sample = HKQuantitySample(
            type: bloodGlucoseType,
            quantity: quantity,
            start: date,
            end: date,
            metadata: metadata
        )

        try await healthStore.save(sample)
    }

    // MARK: - 查询血糖数据

    /// 查询指定日期范围内的血糖记录
    func fetchGlucoseRecords(from startDate: Date, to endDate: Date) async throws -> [HKQuantitySample] {
        guard isAvailable, isAuthorized else { return [] }

        let bloodGlucoseType = HKQuantityType(.bloodGlucose)
        let predicate = HKQuery.predicateForSamples(
            withStart: startDate,
            end: endDate,
            options: .strictStartDate
        )
        let sortDescriptor = SortDescriptor(\HKSample.startDate, order: .reverse)

        let descriptor = HKSampleQueryDescriptor(
            predicates: [.quantitySample(type: bloodGlucoseType, predicate: predicate)],
            sortDescriptors: [sortDescriptor]
        )

        return try await descriptor.result(for: healthStore)
    }

    // MARK: - 统计查询

    /// 获取指定范围内的平均血糖
    func fetchAverageGlucose(from startDate: Date, to endDate: Date) async throws -> Double? {
        guard isAvailable, isAuthorized else { return nil }

        let bloodGlucoseType = HKQuantityType(.bloodGlucose)
        let predicate = HKQuery.predicateForSamples(
            withStart: startDate,
            end: endDate,
            options: .strictStartDate
        )

        let descriptor = HKStatisticsQueryDescriptor(
            predicate: .quantitySample(type: bloodGlucoseType, predicate: predicate),
            options: .discreteAverage
        )

        let result = try await descriptor.result(for: healthStore)
        let unit = HKUnit.moleUnit(with: .milli, molarMass: HKUnitMolarMassBloodGlucose)
            .unitDivided(by: .liter())
        return result?.averageQuantity()?.doubleValue(for: unit)
    }
}
```

### 4.3 MealContext 与 HealthKit 映射

```swift
extension MealContext {
    /// 映射到 HealthKit 的 HKBloodGlucoseMealTime
    var healthKitMealTime: Int? {
        switch self {
        case .beforeBreakfast, .beforeLunch, .beforeDinner, .fasting:
            return HKBloodGlucoseMealTime.preprandial.rawValue  // 餐前
        case .afterBreakfast, .afterLunch, .afterDinner:
            return HKBloodGlucoseMealTime.postprandial.rawValue // 餐后
        case .bedtime, .other:
            return nil  // HealthKit 无对应类型，不设置
        }
    }
}
```

### 4.4 防重复策略

```swift
/// 检查 HealthKit 中是否已存在相同时间戳和来源的记录
func isDuplicate(date: Date) async throws -> Bool {
    let bloodGlucoseType = HKQuantityType(.bloodGlucose)

    // 精确到秒级匹配
    let startDate = Calendar.current.date(byAdding: .second, value: -1, to: date)!
    let endDate = Calendar.current.date(byAdding: .second, value: 1, to: date)!

    let predicate = NSCompoundPredicate(andPredicateWithSubpredicates: [
        HKQuery.predicateForSamples(withStart: startDate, end: endDate),
        HKQuery.predicateForObjects(from: HKSource.default()) // 仅查本 App 写入
    ])

    let descriptor = HKSampleQueryDescriptor(
        predicates: [.quantitySample(type: bloodGlucoseType, predicate: predicate)],
        sortDescriptors: [],
        limit: 1
    )

    let results = try await descriptor.result(for: healthStore)
    return !results.isEmpty
}
```

### 4.5 Entitlements 配置

在 `XueTangJiLu.entitlements` 中需新增 HealthKit 权限：

```xml
<key>com.apple.developer.healthkit</key>
<true/>
<key>com.apple.developer.healthkit.access</key>
<array>
    <string>health-records</string>
</array>
```

---

## 5. CloudKit / SwiftData 同步架构

### 5.1 ModelContainer 配置

```swift
import SwiftUI
import SwiftData

@main
struct XueTangJiLuApp: App {
    let modelContainer: ModelContainer

    init() {
        let schema = Schema([
            GlucoseRecord.self,
            UserSettings.self,
        ])

        let modelConfiguration = ModelConfiguration(
            schema: schema,
            isStoredInMemoryOnly: false,
            // CloudKit 容器标识符
            cloudKitDatabase: .private("iCloud.com.xxl.XueTangJiLu")
        )

        do {
            modelContainer = try ModelContainer(
                for: schema,
                configurations: [modelConfiguration]
            )
        } catch {
            fatalError("无法创建 ModelContainer: \(error)")
        }
    }

    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .modelContainer(modelContainer)
    }
}
```

### 5.2 同步架构图

```
┌──────────────┐     SwiftData      ┌──────────────┐
│   iPhone A   │ ←─────────────────→ │   iCloud     │
│  (本地 DB)    │   Private CloudKit  │  Private DB  │
└──────────────┘                     └──────┬───────┘
                                            │
┌──────────────┐     SwiftData      ┌───────▼──────┐
│   iPhone B   │ ←─────────────────→ │   iCloud     │
│  (本地 DB)    │   Private CloudKit  │  Private DB  │
└──────────────┘                     └──────────────┘

※ 中国区 iCloud 由"云上贵州"运营，数据存储在中国境内
※ 开发者无法访问用户的 Private Database
```

### 5.3 同步策略

| 策略 | 说明 |
|:-----|:-----|
| **离线优先** | 所有操作先写入本地 SwiftData，CloudKit 在网络可用时异步同步 |
| **冲突解决** | 采用 Server Wins 策略（SwiftData + CloudKit 默认行为） |
| **最终一致性** | 不要求实时同步，容忍数秒至数分钟延迟 |
| **增量同步** | CloudKit 仅传输变更数据（CKRecord Change Token） |

### 5.4 数据迁移原则

CloudKit 一旦启用，模型变更受到严格限制：

| 操作 | 是否允许 | 说明 |
|:-----|:---------|:-----|
| 新增属性（有默认值） | 允许 | 旧数据自动填充默认值 |
| 新增 Optional 属性 | 允许 | 旧数据该字段为 nil |
| 删除属性 | **禁止** | 会导致同步数据丢失 |
| 重命名属性 | **禁止** | CloudKit 视为删旧增新 |
| 修改属性类型 | **禁止** | 类型不兼容导致崩溃 |
| 新增 unique 约束 | **禁止** | CloudKit 不支持原子唯一性检查 |

> **最佳实践：** 每次发版前在 CloudKit Dashboard 测试 Schema 迁移，确保生产环境安全。

### 5.5 App Groups 共享数据

Widget Extension 需要读取主 App 的 SwiftData 数据库，需配置 App Groups：

```swift
// 在主 App 和 Widget Extension 中使用相同的 App Group
let modelConfiguration = ModelConfiguration(
    schema: schema,
    isStoredInMemoryOnly: false,
    groupContainer: .identifier("group.com.xxl.XueTangJiLu"),
    cloudKitDatabase: .private("iCloud.com.xxl.XueTangJiLu")
)
```

---

## 6. 核心业务逻辑

### 6.1 场景标签自动匹配引擎 (TagEngine)

根据当前时间自动推断最可能的用餐场景标签，减少用户点击。

```swift
struct TagEngine {

    /// 时间段定义（可在设置中自定义）
    struct TimeSlot {
        let start: Int  // 24小时制起始小时
        let end: Int    // 24小时制结束小时
        let context: MealContext
    }

    /// 默认时间段配置
    static let defaultSlots: [TimeSlot] = [
        TimeSlot(start: 5,  end: 8,  context: .beforeBreakfast),  // 05:00 - 07:59
        TimeSlot(start: 8,  end: 10, context: .afterBreakfast),   // 08:00 - 09:59
        TimeSlot(start: 10, end: 12, context: .beforeLunch),      // 10:00 - 11:59
        TimeSlot(start: 12, end: 14, context: .afterLunch),       // 12:00 - 13:59
        TimeSlot(start: 14, end: 17, context: .beforeDinner),     // 14:00 - 16:59
        TimeSlot(start: 17, end: 20, context: .afterDinner),      // 17:00 - 19:59
        TimeSlot(start: 20, end: 22, context: .bedtime),          // 20:00 - 21:59
        TimeSlot(start: 22, end: 5,  context: .fasting),          // 22:00 - 04:59
    ]

    /// 根据当前时间推断场景标签
    static func suggestContext(for date: Date = .now) -> MealContext {
        let hour = Calendar.current.component(.hour, from: date)

        for slot in defaultSlots {
            if slot.start <= slot.end {
                // 正常范围（如 5-8）
                if hour >= slot.start && hour < slot.end {
                    return slot.context
                }
            } else {
                // 跨午夜范围（如 22-5）
                if hour >= slot.start || hour < slot.end {
                    return slot.context
                }
            }
        }
        return .other
    }
}
```

### 6.2 单位智能识别

```swift
struct GlucoseUnitConverter {

    /// mmol/L 转 mg/dL
    static func toMgDL(_ mmolL: Double) -> Double {
        mmolL * 18.0182
    }

    /// mg/dL 转 mmol/L
    static func toMmolL(_ mgdL: Double) -> Double {
        mgdL / 18.0182
    }

    /// 智能识别输入值的单位
    /// 规则：mmol/L 通常范围 1.0 - 33.3，mg/dL 通常范围 18 - 600
    /// 如果值 > 35，大概率是 mg/dL
    static func detectUnit(for value: Double) -> GlucoseUnit {
        if value > 35.0 {
            return .mgdL
        } else {
            return .mmolL
        }
    }

    /// 将任意输入标准化为 mmol/L（内部统一存储单位）
    static func normalize(value: Double, preferredUnit: GlucoseUnit) -> Double {
        switch preferredUnit {
        case .mmolL: return value
        case .mgdL:  return toMmolL(value)
        }
    }

    /// 格式化显示值
    static func displayString(mmolLValue: Double, in unit: GlucoseUnit) -> String {
        switch unit {
        case .mmolL:
            return String(format: "%.1f", mmolLValue)
        case .mgdL:
            return String(format: "%.0f", toMgDL(mmolLValue))
        }
    }
}
```

### 6.3 关键指标计算 (GlucoseCalculator)

```swift
struct GlucoseCalculator {

    /// 计算预估平均血糖 (eAG)
    /// 公式：eAG (mmol/L) = 所有记录的算术平均值
    static func estimatedAverageGlucose(records: [GlucoseRecord]) -> Double? {
        guard !records.isEmpty else { return nil }
        let sum = records.reduce(0.0) { $0 + $1.value }
        return sum / Double(records.count)
    }

    /// 计算预估糖化血红蛋白 (eA1C)
    /// 公式：eA1C (%) = (eAG(mg/dL) + 46.7) / 28.7
    /// 即：eA1C (%) = (eAG(mmol/L) × 18.0182 + 46.7) / 28.7
    static func estimatedA1C(averageGlucoseMmolL: Double) -> Double {
        let eAGmgdL = averageGlucoseMmolL * 18.0182
        return (eAGmgdL + 46.7) / 28.7
    }

    /// 计算达标时间比率 (Time in Range, TIR)
    /// 达标范围默认 3.9 - 10.0 mmol/L（可自定义）
    static func timeInRange(
        records: [GlucoseRecord],
        low: Double = 3.9,
        high: Double = 10.0
    ) -> Double {
        guard !records.isEmpty else { return 0.0 }
        let inRange = records.filter { $0.value >= low && $0.value <= high }
        return Double(inRange.count) / Double(records.count) * 100.0
    }

    /// 计算血糖标准差（反映波动程度）
    static func standardDeviation(records: [GlucoseRecord]) -> Double? {
        guard records.count > 1,
              let avg = estimatedAverageGlucose(records: records) else {
            return nil
        }
        let variance = records.reduce(0.0) { $0 + pow($1.value - avg, 2) }
            / Double(records.count - 1)
        return sqrt(variance)
    }

    /// 计算变异系数 (CV%)
    /// CV < 36% 认为血糖波动稳定
    static func coefficientOfVariation(records: [GlucoseRecord]) -> Double? {
        guard let avg = estimatedAverageGlucose(records: records),
              let sd = standardDeviation(records: records),
              avg > 0 else {
            return nil
        }
        return (sd / avg) * 100.0
    }
}
```

### 6.4 PDF 报告生成 (PDFExportService)

```swift
import UIKit
import PDFKit

struct PDFExportService {

    /// 生成血糖报告 PDF
    /// - Parameters:
    ///   - records: 血糖记录列表
    ///   - dateRange: 日期范围描述
    ///   - unit: 显示单位
    /// - Returns: PDF 文件的 Data
    static func generateReport(
        records: [GlucoseRecord],
        dateRange: String,
        unit: GlucoseUnit
    ) -> Data {
        let pageWidth: CGFloat = 595.2   // A4 宽度 (pt)
        let pageHeight: CGFloat = 841.8  // A4 高度 (pt)
        let margin: CGFloat = 40.0

        let pdfRenderer = UIGraphicsPDFRenderer(
            bounds: CGRect(x: 0, y: 0, width: pageWidth, height: pageHeight)
        )

        return pdfRenderer.pdfData { context in
            context.beginPage()
            var yOffset: CGFloat = margin

            // 1. 标题
            let title = "血糖记录报告"
            let titleAttributes: [NSAttributedString.Key: Any] = [
                .font: UIFont.systemFont(ofSize: 24, weight: .bold),
                .foregroundColor: UIColor.label
            ]
            title.draw(at: CGPoint(x: margin, y: yOffset), withAttributes: titleAttributes)
            yOffset += 40

            // 2. 日期范围
            let subtitle = "报告周期：\(dateRange)"
            let subtitleAttributes: [NSAttributedString.Key: Any] = [
                .font: UIFont.systemFont(ofSize: 14),
                .foregroundColor: UIColor.secondaryLabel
            ]
            subtitle.draw(at: CGPoint(x: margin, y: yOffset), withAttributes: subtitleAttributes)
            yOffset += 30

            // 3. 统计概要
            if let avg = GlucoseCalculator.estimatedAverageGlucose(records: records) {
                let tir = GlucoseCalculator.timeInRange(records: records)
                let a1c = GlucoseCalculator.estimatedA1C(averageGlucoseMmolL: avg)

                let summary = """
                记录次数：\(records.count)
                平均血糖：\(GlucoseUnitConverter.displayString(mmolLValue: avg, in: unit)) \(unit.rawValue)
                达标率 (TIR)：\(String(format: "%.1f%%", tir))
                预估 A1C：\(String(format: "%.1f%%", a1c))
                """

                let summaryAttributes: [NSAttributedString.Key: Any] = [
                    .font: UIFont.systemFont(ofSize: 12),
                    .foregroundColor: UIColor.label
                ]
                summary.draw(
                    in: CGRect(x: margin, y: yOffset, width: pageWidth - margin * 2, height: 100),
                    withAttributes: summaryAttributes
                )
                yOffset += 100
            }

            // 4. 逐条数据表格
            // ... 表头绘制 + 逐行数据渲染 + 翻页逻辑 ...

            // 5. 免责声明
            let disclaimer = "免责声明：本报告仅用于信息记录，不作为医疗诊断依据。做出医疗决定前请咨询医生。"
            let disclaimerAttributes: [NSAttributedString.Key: Any] = [
                .font: UIFont.systemFont(ofSize: 9),
                .foregroundColor: UIColor.tertiaryLabel
            ]
            disclaimer.draw(
                in: CGRect(x: margin, y: pageHeight - 60, width: pageWidth - margin * 2, height: 40),
                withAttributes: disclaimerAttributes
            )
        }
    }
}
```

---

## 7. Widget / Siri Shortcuts 实现

### 7.1 WidgetKit 架构

```
┌─────────────────────────────────────┐
│         Widget Extension            │
│                                     │
│  ┌─────────────────────────────┐    │
│  │   WidgetTimelineProvider    │    │
│  │  - 从 SwiftData 读取最新记录  │    │
│  │  - 生成 Timeline Entry       │    │
│  └─────────┬───────────────────┘    │
│            │                        │
│  ┌─────────▼───────────────────┐    │
│  │       Widget Views          │    │
│  │  - SmallWidget (最新读数)     │    │
│  │  - MediumWidget (读数+趋势)   │    │
│  │  - LockScreenWidget (TIR)   │    │
│  └─────────────────────────────┘    │
│                                     │
│  ※ 通过 App Groups 共享 SwiftData   │
└─────────────────────────────────────┘
```

### 7.2 Timeline Provider

```swift
import WidgetKit
import SwiftData

struct GlucoseTimelineProvider: TimelineProvider {

    typealias Entry = GlucoseWidgetEntry

    func placeholder(in context: Context) -> GlucoseWidgetEntry {
        GlucoseWidgetEntry(
            date: .now,
            latestValue: 5.6,
            latestTime: .now,
            unit: .mmolL,
            level: .normal,
            weekTrend: []
        )
    }

    func getSnapshot(in context: Context, completion: @escaping (GlucoseWidgetEntry) -> Void) {
        completion(placeholder(in: context))
    }

    func getTimeline(in context: Context, completion: @escaping (Timeline<GlucoseWidgetEntry>) -> Void) {
        // 从 App Groups 的 SwiftData 容器读取数据
        let entry = fetchLatestEntry()

        // 每 30 分钟刷新一次
        let nextUpdate = Calendar.current.date(byAdding: .minute, value: 30, to: .now)!
        let timeline = Timeline(entries: [entry], policy: .after(nextUpdate))
        completion(timeline)
    }

    private func fetchLatestEntry() -> GlucoseWidgetEntry {
        // SwiftData 查询逻辑，通过 App Groups 共享数据库
        // 获取最新记录 + 最近 7 天趋势数据
        // ...
        return GlucoseWidgetEntry(
            date: .now,
            latestValue: 5.6,
            latestTime: .now,
            unit: .mmolL,
            level: .normal,
            weekTrend: []
        )
    }
}

struct GlucoseWidgetEntry: TimelineEntry {
    let date: Date
    let latestValue: Double
    let latestTime: Date
    let unit: GlucoseUnit
    let level: GlucoseLevel
    let weekTrend: [(Date, Double)]  // 7日趋势数据点
}
```

### 7.3 Widget 视图（小号示例）

```swift
struct SmallGlucoseWidget: View {
    let entry: GlucoseWidgetEntry

    var body: some View {
        VStack(alignment: .leading, spacing: 4) {
            Text(GlucoseUnitConverter.displayString(
                mmolLValue: entry.latestValue, in: entry.unit
            ))
            .font(.system(size: 36, weight: .bold, design: .rounded))
            .foregroundStyle(Color(entry.level.colorName))

            Text(entry.unit.rawValue)
                .font(.caption2)
                .foregroundStyle(.secondary)

            Spacer()

            Text(entry.latestTime, style: .relative)
                .font(.caption)
                .foregroundStyle(.secondary)
        }
        .padding()
        .containerBackground(.fill.tertiary, for: .widget)
    }
}
```

### 7.4 Siri Shortcuts (App Intents)

```swift
import AppIntents

struct RecordGlucoseIntent: AppIntent {
    static var title: LocalizedStringResource = "记录血糖"
    static var description = IntentDescription("快速记录一次血糖数值")
    static var openAppWhenRun = false

    @Parameter(title: "血糖值")
    var glucoseValue: Double

    func perform() async throws -> some IntentResult & ProvidesDialog {
        // 1. 将值保存到 SwiftData（通过 App Groups 共享容器）
        // 2. 同步到 HealthKit
        // 3. 刷新 Widget

        let context = TagEngine.suggestContext()

        return .result(
            dialog: "已记录血糖 \(String(format: "%.1f", glucoseValue)) mmol/L（\(context.displayName)）"
        )
    }
}

// Siri 快捷短语
struct RecordGlucoseShortcuts: AppShortcutsProvider {
    static var appShortcuts: [AppShortcut] {
        AppShortcut(
            intent: RecordGlucoseIntent(),
            phrases: [
                "用\(.applicationName)记录血糖",
                "记录血糖 \(\.$glucoseValue)"
            ],
            shortTitle: "记录血糖",
            systemImageName: "drop.fill"
        )
    }
}
```

---

## 8. 数据隐私与合规

### 8.1 Info.plist 权限配置

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- HealthKit 权限 -->
    <key>NSHealthShareUsageDescription</key>
    <string>学糖记录需要读取您的血糖数据，以便在应用内展示完整的血糖记录历史。</string>

    <key>NSHealthUpdateUsageDescription</key>
    <string>学糖记录需要将您手动输入的血糖数据同步到"健康"应用中，确保数据安全备份。</string>

    <!-- 后台模式 -->
    <key>UIBackgroundModes</key>
    <array>
        <string>remote-notification</string>
    </array>
</dict>
</plist>
```

### 8.2 Entitlements 完整配置

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- 推送通知 -->
    <key>aps-environment</key>
    <string>development</string>

    <!-- CloudKit -->
    <key>com.apple.developer.icloud-container-identifiers</key>
    <array>
        <string>iCloud.com.xxl.XueTangJiLu</string>
    </array>
    <key>com.apple.developer.icloud-services</key>
    <array>
        <string>CloudKit</string>
    </array>

    <!-- HealthKit -->
    <key>com.apple.developer.healthkit</key>
    <true/>
    <key>com.apple.developer.healthkit.access</key>
    <array/>

    <!-- App Groups (Widget 共享数据) -->
    <key>com.apple.security.application-groups</key>
    <array>
        <string>group.com.xxl.XueTangJiLu</string>
    </array>
</dict>
</plist>
```

### 8.3 隐私清单 (PrivacyInfo.xcprivacy)

从 2024 年春季起，Apple 要求所有应用提交隐私清单文件。

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>NSPrivacyTracking</key>
    <false/>

    <key>NSPrivacyTrackingDomains</key>
    <array/>

    <key>NSPrivacyCollectedDataTypes</key>
    <array>
        <dict>
            <key>NSPrivacyCollectedDataType</key>
            <string>NSPrivacyCollectedDataTypeHealthData</string>
            <key>NSPrivacyCollectedDataTypeLinked</key>
            <false/>
            <key>NSPrivacyCollectedDataTypeTracking</key>
            <false/>
            <key>NSPrivacyCollectedDataTypePurposes</key>
            <array>
                <string>NSPrivacyCollectedDataTypePurposeAppFunctionality</string>
            </array>
        </dict>
    </array>

    <key>NSPrivacyAccessedAPITypes</key>
    <array>
        <dict>
            <key>NSPrivacyAccessedAPIType</key>
            <string>NSPrivacyAccessedAPICategoryUserDefaults</string>
            <key>NSPrivacyAccessedAPITypeReasons</key>
            <array>
                <string>CA92.1</string>
            </array>
        </dict>
    </array>
</dict>
</plist>
```

### 8.4 免责声明

必须在以下位置展示免责声明：

| 展示位置 | 触发时机 | 文案 |
|:---------|:---------|:-----|
| **首次引导** | Onboarding 第一页 | "本应用仅用于个人健康数据记录，不提供任何医疗诊断或治疗建议。请在做出任何医疗决定前咨询您的医生。" |
| **设置 > 关于** | 用户主动查看 | 完整免责声明 + 隐私政策链接 |
| **PDF 报告页脚** | 导出 PDF 时 | "本报告仅用于信息记录，不作为医疗诊断依据。" |
| **App Store 描述** | 用户下载前 | "本应用为个人健康日志工具，不替代专业医疗建议。" |

### 8.5 数据安全架构

```
┌────────────────────────────────────────────────────┐
│                   数据流向                           │
│                                                    │
│   用户输入 → SwiftData (设备本地) → CloudKit (iCloud) │
│                ↓                                    │
│           HealthKit (设备本地)                       │
│                                                    │
│   ✗ 无第三方服务器                                    │
│   ✗ 无用户账号体系                                    │
│   ✗ 无数据分析/追踪 SDK                               │
│   ✗ 无广告 SDK                                       │
│   ✓ 所有数据加密存储（iOS 系统级加密）                  │
│   ✓ iCloud 传输使用 TLS 加密                         │
│   ✓ 用户可随时删除所有数据                             │
└────────────────────────────────────────────────────┘
```

---

## 9. 性能优化策略

### 9.1 冷启动优化（3 秒录入目标）

| 优化点 | 具体措施 | 预期效果 |
|:-------|:---------|:---------|
| **启动耗时** | ModelContainer 在 App init 中同步初始化，避免首帧延迟 | < 500ms |
| **首屏渲染** | 首屏仅加载最近 20 条记录，无网络请求 | < 200ms |
| **录入路径** | 录入按钮常驻首页，点击后 sheet 弹出自定义键盘（无系统键盘弹起延迟）| < 300ms |
| **触觉反馈** | 使用预准备的 UIImpactFeedbackGenerator（在 viewDidAppear 时 prepare）| 即时 |

### 9.2 列表性能

```swift
// 使用 LazyVStack 替代 List 以获得更好的滚动性能
ScrollView {
    LazyVStack(spacing: 0) {
        ForEach(records) { record in
            TimelineRowView(record: record)
                .id(record.persistentModelID)
        }
    }
}
```

**分页加载策略：**

```swift
/// 首次加载最近 50 条
/// 用户滚动到底部时，再加载前 50 条
/// 使用 SwiftData 的 fetchLimit + fetchOffset

@Query(
    filter: #Predicate<GlucoseRecord> { _ in true },
    sort: \GlucoseRecord.timestamp,
    order: .reverse
)
private var recentRecords: [GlucoseRecord]

// 初始 fetchLimit = 50
// 当列表滚动到倒数第 5 条时，触发下一批加载
```

### 9.3 图表数据聚合

对于长时间范围（30 天）的趋势图表，避免绘制每个数据点：

```swift
/// 将原始数据按天聚合为平均值
static func aggregateByDay(records: [GlucoseRecord]) -> [(Date, Double)] {
    let calendar = Calendar.current
    let grouped = Dictionary(grouping: records) { record in
        calendar.startOfDay(for: record.timestamp)
    }

    return grouped.map { (date, dayRecords) in
        let avg = dayRecords.reduce(0.0) { $0 + $1.value } / Double(dayRecords.count)
        return (date, avg)
    }
    .sorted { $0.0 < $1.0 }
}

/// 30 天趋势图使用聚合数据（最多 30 个点）
/// 7 天趋势图使用原始数据（通常 < 100 个点）
```

### 9.4 内存管理

```swift
// SwiftData 查询使用 @Query 宏自动管理内存
// 避免在 ViewModel 中缓存大量 Model 对象

// 图表数据使用值类型（struct）而非引用类型
struct ChartDataPoint: Identifiable {
    let id = UUID()
    let date: Date
    let value: Double
    let level: GlucoseLevel
}
```

---

## 10. 测试策略

### 10.1 测试分层

```
┌─────────────────────────────────────┐
│          UI Tests (E2E)             │
│   录入流程 / 图表交互 / 导航流转       │
├─────────────────────────────────────┤
│        Integration Tests            │
│   HealthKit 读写 / SwiftData CRUD   │
├─────────────────────────────────────┤
│          Unit Tests                 │
│  ViewModel / Calculator / TagEngine │
│  UnitConverter / PDF Export         │
└─────────────────────────────────────┘
```

### 10.2 单元测试覆盖

| 测试模块 | 测试内容 | 关键测试用例 |
|:---------|:---------|:-------------|
| **GlucoseUnitConverter** | 单位转换精度 | `5.5 mmol/L = 99.1 mg/dL`、边界值、零值 |
| **TagEngine** | 时间段标签匹配 | 各时间段正确匹配、跨午夜场景、边界时间 |
| **GlucoseCalculator** | eAG / TIR / A1C | 空数组处理、单条记录、大数据集精度 |
| **GlucoseRecord** | glucoseLevel 判定 | 各阈值边界值（3.8/3.9/6.9/7.0/9.9/10.0）|
| **GlucoseViewModel** | 录入/删除逻辑 | 成功/失败路径、输入验证 |

### 10.3 单元测试示例

```swift
import Testing
@testable import XueTangJiLu

struct TagEngineTests {

    @Test("早晨 7 点应推荐早餐前标签")
    func morningTag() {
        let date = createDate(hour: 7, minute: 30)
        let context = TagEngine.suggestContext(for: date)
        #expect(context == .beforeBreakfast)
    }

    @Test("午夜应推荐空腹标签")
    func midnightTag() {
        let date = createDate(hour: 0, minute: 0)
        let context = TagEngine.suggestContext(for: date)
        #expect(context == .fasting)
    }

    @Test("下午 6 点应推荐晚餐后标签")
    func eveningTag() {
        let date = createDate(hour: 18, minute: 0)
        let context = TagEngine.suggestContext(for: date)
        #expect(context == .afterDinner)
    }

    private func createDate(hour: Int, minute: Int) -> Date {
        var components = Calendar.current.dateComponents(
            [.year, .month, .day], from: .now
        )
        components.hour = hour
        components.minute = minute
        return Calendar.current.date(from: components)!
    }
}

struct GlucoseCalculatorTests {

    @Test("TIR 计算 - 全部达标")
    func tirAllInRange() {
        let records = [5.0, 6.0, 7.0].map {
            GlucoseRecord(value: $0)
        }
        let tir = GlucoseCalculator.timeInRange(records: records)
        #expect(tir == 100.0)
    }

    @Test("TIR 计算 - 部分达标")
    func tirPartialInRange() {
        let records = [3.0, 5.0, 12.0, 6.5].map {
            GlucoseRecord(value: $0)
        }
        let tir = GlucoseCalculator.timeInRange(records: records)
        #expect(tir == 50.0)
    }

    @Test("eA1C 计算精度")
    func estimatedA1C() {
        // 平均血糖 7.0 mmol/L ≈ 126.1 mg/dL → eA1C ≈ 6.0%
        let a1c = GlucoseCalculator.estimatedA1C(averageGlucoseMmolL: 7.0)
        #expect(a1c > 5.9 && a1c < 6.1)
    }

    @Test("空数组应返回 nil")
    func emptyRecords() {
        let avg = GlucoseCalculator.estimatedAverageGlucose(records: [])
        #expect(avg == nil)
    }
}

struct UnitConverterTests {

    @Test("mmol/L 转 mg/dL")
    func mmolToMgdl() {
        let result = GlucoseUnitConverter.toMgDL(5.5)
        #expect(abs(result - 99.1) < 0.1)
    }

    @Test("智能单位检测 - 小数值识别为 mmol/L")
    func detectMmolL() {
        let unit = GlucoseUnitConverter.detectUnit(for: 5.5)
        #expect(unit == .mmolL)
    }

    @Test("智能单位检测 - 大数值识别为 mg/dL")
    func detectMgdL() {
        let unit = GlucoseUnitConverter.detectUnit(for: 120)
        #expect(unit == .mgdL)
    }
}
```

### 10.4 UI 测试

```swift
import XCTest

final class RecordFlowUITests: XCTestCase {

    let app = XCUIApplication()

    override func setUp() {
        continueAfterFailure = false
        app.launchArguments.append("--uitesting")
        app.launch()
    }

    func testQuickRecordFlow() {
        // 1. 点击录入按钮
        let recordButton = app.buttons["addRecord"]
        XCTAssertTrue(recordButton.waitForExistence(timeout: 3))
        recordButton.tap()

        // 2. 输入血糖值 5.6
        app.buttons["keypad_5"].tap()
        app.buttons["keypad_dot"].tap()
        app.buttons["keypad_6"].tap()

        // 3. 验证预览显示
        let preview = app.staticTexts["glucosePreview"]
        XCTAssertEqual(preview.label, "5.6")

        // 4. 点击保存
        app.buttons["saveRecord"].tap()

        // 5. 验证返回首页并显示新记录
        let latestRecord = app.staticTexts["latestGlucoseValue"]
        XCTAssertTrue(latestRecord.waitForExistence(timeout: 2))
        XCTAssertEqual(latestRecord.label, "5.6")
    }
}
```

### 10.5 HealthKit 模拟测试

```swift
/// 使用 Mock 对象测试 HealthKit 相关逻辑
/// 实际 HKHealthStore 无法在模拟器单元测试中使用

protocol HealthKitManaging {
    var isAvailable: Bool { get }
    func requestAuthorization() async throws
    func saveGlucose(value: Double, date: Date, mealContext: MealContext) async throws
}

/// 生产环境实现
extension HealthKitManager: HealthKitManaging {}

/// 测试 Mock
class MockHealthKitManager: HealthKitManaging {
    var isAvailable = true
    var savedRecords: [(Double, Date, MealContext)] = []

    func requestAuthorization() async throws {
        // 模拟成功
    }

    func saveGlucose(value: Double, date: Date, mealContext: MealContext) async throws {
        savedRecords.append((value, date, mealContext))
    }
}
```

---

## 附录

### A. 血糖范围参考值

| 场景 | 正常范围 (mmol/L) | 正常范围 (mg/dL) |
|:-----|:------------------|:-----------------|
| 空腹 | 3.9 - 6.1 | 70 - 110 |
| 餐后 2 小时 | 3.9 - 7.8 | 70 - 140 |
| 糖尿病控制目标 | 4.4 - 7.0 (空腹) | 80 - 130 (空腹) |
| 低血糖 | < 3.9 | < 70 |
| 严重高血糖 | > 13.9 | > 250 |

### B. 关键 API 版本兼容性矩阵

| API | 最低 iOS 版本 | 备注 |
|:----|:-------------|:-----|
| SwiftUI | iOS 13 | 基础 UI 框架 |
| SwiftData | iOS 17 | 数据持久化 |
| Swift Charts | iOS 16 | 图表（iOS 17 改进） |
| WidgetKit | iOS 14 | 桌面小组件 |
| Lock Screen Widget | iOS 16 | 锁屏小组件 |
| App Intents | iOS 16 | Siri Shortcuts |
| Observation (@Observable) | iOS 17 | MVVM 状态管理 |
| HealthKit (async/await) | iOS 15.4 | 异步 API |

### C. 第三方依赖

**无。** 本项目完全基于 Apple 原生框架，零第三方依赖。

优势：
- 无供应链安全风险
- 无版本兼容性问题
- 包体积最小化
- App Store 审核友好
